# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                    UnrealMate - CI/CD Test Workflow                          ║
# ║                                                                              ║
# ║  Author: gktrk363                                                            ║
# ║  GitHub: https://github.com/gktrk363/unrealmate                              ║
# ║  Purpose: Automated testing on push and pull requests                        ║
# ╚══════════════════════════════════════════════════════════════════════════════╝
#
# © 2026 gktrk363 - Crafted with passion for Unreal Engine developers

name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run linting with Ruff
        run: |
          ruff check unrealmate/ tests/
        continue-on-error: true

      - name: Run tests with pytest
        run: |
          pytest tests/ -v --tb=short

      - name: Run tests with coverage
        run: |
          pytest tests/ --cov=unrealmate --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          fail_ci_if_error: false

  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff mypy

      - name: Check formatting with Ruff
        run: ruff format --check unrealmate/ tests/
        continue-on-error: true

      - name: Check linting with Ruff
        run: ruff check unrealmate/ tests/
        continue-on-error: true

      - name: Type check with mypy
        run: mypy unrealmate/ --ignore-missing-imports
        continue-on-error: true
