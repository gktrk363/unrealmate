# UnrealMate Generated GitHub Actions Workflow
# © 2026 gktrk363

name: Unreal Engine Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3
      with:
        lfs: true
    
    - name: Setup Unreal Engine
      run: |
        # Add your UE installation path
        echo "UE_ROOT=C:\\Program Files\\Epic Games\\UE_5.3" >> $GITHUB_ENV
    
    - name: Build Project
      run: |
        $UE_ROOT\\Engine\\Build\\BatchFiles\\RunUAT.bat BuildCookRun `
          -project="${{ github.workspace }}\\.uproject" `
          -platform=Win64 `
          -clientconfig=Development `
          -cook `
          -build `
          -stage `
          -pak `
          -archive `
          -archivedirectory="${{ github.workspace }}\\Build"
    
    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v3
      with:
        name: -Build
        path: Build/
    
    - name: Run Tests (Optional)
      run: |
        # Add your test commands here
        echo "Running tests..."

# Crafted with UnrealMate by gktrk363
